# ── LLM ──────────────────────────────────────────────────────────────────────
MODEL=openrouter/meta-llama/llama-3.3-70b-instruct   # dev default; switch to claude-sonnet for prod
OPENROUTER_API_KEY=your_key_here

# Alternative: use Gemini directly (free tier)
# MODEL=gemini/gemini-1.5-flash
# GEMINI_API_KEY=your_key_here

# ── AutoOptimiser Strategist (Tier 3) ────────────────────────────────────────
# Routes through OPENROUTER_API_KEY above — no separate Anthropic key needed.
# Override models if you want a different Claude tier:
# AO_STRATEGIST_DAILY_MODEL=openrouter/anthropic/claude-sonnet-4-5
# AO_STRATEGIST_WEEKLY_MODEL=openrouter/anthropic/claude-opus-4-5

# ── AutoOptimiser Threshold Overrides ────────────────────────────────────────
# Leave commented to use compiled defaults. Uncomment + set to override.
# The Tier-2 learning loop (optimization_tracker) self-calibrates these over time.
# AO_BENCHMARK_CPL=85        # best-ever CPL anchor in ₹
# AO_CPL_CEILING=500         # bleeding CPL threshold in ₹
# AO_FREQ_SATURATED=3.0      # audience fatigue threshold (Meta frequency)
# AO_FREQ_EXHAUSTED=5.0      # pause-consideration frequency threshold
# AO_CPL_RISING_RATIO=1.3    # 7d/30d CPL ratio flagging deterioration
# AO_QUALITY_LEAD_MIN=5      # min quality leads to trust quality-CPL
# AO_COOLDOWN_DAYS=5         # days between stacking fixes on the same campaign

# ── Search ───────────────────────────────────────────────────────────────────
SERPER_API_KEY=your_key_here          # SerperDev — used by audience crew agents

# ── Meta Ads (Phase 3 — Task 3.1) ────────────────────────────────────────────
# META_ACCESS_TOKEN=your_token_here   # Long-lived token (60-day expiry — set a calendar reminder)
# META_AD_ACCOUNT_ID=act_XXXXXXXXXX   # Format: act_ prefix + numeric ID — from Business Suite → Settings → Ad Accounts
# META_PAGE_ID=your_page_id           # From Business Suite → Settings → Pages
# META_PIXEL_ID=your_pixel_id         # From Meta Events Manager → Data Sources (optional, for conversion tracking)
# DRY_RUN=true                        # Set to false ONLY when ready for live deployment
# META_APP_ID=your_app_id
# META_APP_SECRET=your_app_secret
# DRY_RUN=true                        # Set to false ONLY when ready for live deployment

# ── Email — Brevo (Phase 3 — Task 3.3) ───────────────────────────────────────
# BREVO_API_KEY=your_key_here

# ── WhatsApp (Phase 3 — Task 3.4) ────────────────────────────────────────────
# INTERAKT_API_KEY=your_key_here      # OR WhatsApp Business Cloud API token

# ── Notifications (optional) ─────────────────────────────────────────────────
# SLACK_WEBHOOK_URL=your_webhook_url  # For human review notifications (Task 1.4)
